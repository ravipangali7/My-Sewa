# Local Django. In production, point at the API host (not the SPA host).
# Leave empty only when nginx same-origin proxies /api on the SPA domain.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Production example (API on separate host):
# VITE_API_BASE_URL=https://mysewaserver.sewabyapar.com
